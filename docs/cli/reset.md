---
summary: "`moltbot reset` 的 CLI 参考（重置本地状态与配置）"
read_when:
  - 想清理本地状态但保留 CLI 安装
  - 想先查看将删除的内容
---

# `moltbot reset`

重置本地配置与状态（CLI 仍保留）。

```bash
moltbot reset
moltbot reset --dry-run
moltbot reset --scope config+creds+sessions --yes --non-interactive
```
