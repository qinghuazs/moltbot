---
summary: "`moltbot sessions` 的 CLI 参考（列出存储的会话与用量）"
read_when:
  - 想列出存储会话并查看近期活动
---

# `moltbot sessions`

列出存储的对话会话。

```bash
moltbot sessions
moltbot sessions --active 120
moltbot sessions --json
```
