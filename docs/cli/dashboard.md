---
summary: "`moltbot dashboard` 的 CLI 参考（打开控制台 UI）"
read_when:
  - 想用当前令牌打开控制台 UI
  - 想仅打印 URL 而不启动浏览器
---

# `moltbot dashboard`

使用当前认证打开控制台 UI。

```bash
moltbot dashboard
moltbot dashboard --no-open
```
